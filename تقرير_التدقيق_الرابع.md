# تقرير التدقيق الرابع — إصلاح كراشات فعلية + مشاكل تصميم RTL

> التاريخ: 2026-09-12
> السياق: التدقيقات الثلاثة السابقة (`FIX_REPORT.md`, `AUDIT_FIX_REPORT.md`, `DEEP_AUDIT_FIX_REPORT.md`, و`تقرير_التدقيق_الثالث.md` المحذوف بعد دمجه) عولجت جميعها في غياب Android SDK/Gradle، وركّزت على أخطاء ترجمة وبنية معمارية. هذه الجولة موجّهة بملاحظات تشغيل فعلية من المستخدم على جهاز حقيقي: **"عند إضافة مجلد يحدث كراش"** و**"عند فتح تبويب الملفات بعد إنشاء ملف يحدث كراش"**، بالإضافة إلى ملاحظة أن بعض عناصر الواجهة "تظهر بشكل غريب".

## السبب الجذري للكراشات (الأرجح والمعالَج)

التطبيق مرّ بعدة تعديلات متتالية لمخطط قاعدة البيانات Room عبر الجلسات السابقة (v1 → v2 → v3 → v4: فهارس ثم إضافة مفتاح أجنبي CASCADE بين `files.folderId` و`folders.id`). أي تثبيت سابق للتطبيق على جهاز الاختبار قبل هذه التعديلات يحمل ملف قاعدة بيانات `uni_manager_db` ببنية جداول قديمة. عند تشغيل الإصدار الجديد فوقه:

- Room يحاول تطبيق `MIGRATION_1_2` → `MIGRATION_2_3` → `MIGRATION_3_4` بالترتيب، ثم يتحقق من مطابقة البنية الناتجة لما تولّده تعريفات الكيانات الحالية حرفياً (أعمدة، أنواع، مفاتيح أجنبية، فهارس).
- أي اختلاف طفيف (مثال: مفتاح المجلدات الذاتي `parentId → folders.id` الموجود في تعريف `FolderEntity` منذ البداية لكن لم تُفحص مطابقته الفعلية على جهاز مرّ بعدة تثبيتات تجريبية) يجعل Room يرمي:
  `IllegalStateException: Migration didn't properly handle folders(...)`
- هذا الاستثناء **غير مُغلَّف** بأي `try/catch` في مسار فتح القاعدة (يحدث في `onOpen` عند أول استعلام)، وأول استعلام يلمس جدول `folders` هو بالضبط شاشة الملفات (تحميل `rootFolders`) وأول إدراج يلمسه هو **إضافة مجلد** — وهو نفس ما أبلغ عنه المستخدم حرفياً.

### الإصلاح
1. **`di/DatabaseModule.kt`**: تغيير اسم ملف القاعدة إلى `uni_manager_db_v2`. بما أن هذا تطبيق شخصي للتجربة (غير منشور)، فإن أي بيانات اختبار قديمة غير حرجة؛ إعطاء القاعدة اسمًا جديدًا يضمن أن Room يبني المخطط مباشرة من التعريفات الحالية عبر `onCreate` (بلا أي مسار ترقية، وبلا أي احتمال لعدم تطابق) — إصلاح ضامن 100% لهذه الفئة من الكراش على الجهاز الحالي.
2. أُبقيت الهجرات الثلاث الصريحة مسجّلة (ممارسة سليمة لأي تحديث مستقبلي)، وأُضيفت `.fallbackToDestructiveMigration()` كشبكة أمان: لو حدث تعارض مخطط جديد مستقبلاً (نسيان كتابة هجرة)، يعاد بناء القاعدة تلقائياً بدل الدخول في حلقة كراش عند كل فتح للتطبيق. تم توثيق أن هذا القرار غير مناسب لتطبيق إنتاجي عام (فيه فقدان بيانات صامت)، لكنه مبرَّر ومطلوب صراحة هنا لأن المستخدم أكد أن التطبيق شخصي وليس للنشر.
3. **`res/xml/backup_rules.xml`** و**`res/xml/data_extraction_rules.xml`**: كانا يشيران لاسم قاعدة البيانات القديم `uni_manager_db`؛ حُدِّثا إلى `uni_manager_db_v2` لمنع استثناء اسم ملف غير موجود مستقبلاً عند تفعيل `allowBackup`.

## طبقة أمان إضافية: حماية تدفقات Room من إسقاط الشاشة كاملة

**`data/repository/AppRepository.kt`**: كل تدفقات `Flow` القادمة من الـ DAOs (القوائم والعدادات) كانت تُغذّي `stateIn(...)` مباشرة في `AppViewModel` دون أي معالجة أخطاء؛ أي استثناء غير متوقع أثناء تنفيذ استعلام (نادر لكن ممكن) كان سيُسقط الكوروتين المُجمِّعة بصمت خارج نطاق أي `try/catch` موجود في الشاشات، فيظهر للمستخدم كتعطّل فوري لشاشة كاملة (كتبويب الملفات الذي يستمع لعدة تدفقات معاً: `rootFolders`, `allFiles`, `getChildFolders`, `getFilesInFolder`).

أُضيف امتداد خاص `Flow<T>.safe(tag, fallback)` يستخدم `catch {}` لتسجيل الخطأ عبر `Log.e` وإصدار قيمة احتياطية آمنة (قائمة فارغة أو صفر) بدل إسقاط التدفق. طُبِّق على **كل** دوال `AppRepository` التي تُعيد `Flow` (16 دالة: مجلدات، ملفات، محاضرات، مهام، ملاحظات، امتحانات وعدّاداتها). النتيجة: حتى لو حدث أي خطأ غير متوقع مستقبلاً في طبقة البيانات، تبقى الواجهة تعمل بقائمة فارغة بدل الانهيار الكامل.

## إصلاح منطقي: مطابقة NULL في استعلامات SQL

**`data/dao/FolderDao.kt`** و**`data/dao/FileDao.kt`**: كانت `getChildFolders(parentId: Long?)` و`getFilesInFolder(folderId: Long?)` تستخدمان `WHERE parentId = :parentId` / `WHERE folderId = :folderId`. في SQLite، مقارنة أي قيمة بـ `NULL` بمعامل `=` تُقيَّم دائماً كـ`UNKNOWN` (لا تُطابق أبداً)، حتى عند تمرير NULL فعلياً كمعامل. النتيجة: استدعاء هاتين الدالتين بمعامل فارغ (تمثيل "المجلد الجذري") كان يُعيد دائماً قائمة فارغة خاطئة منطقياً، وكان يُخفى فقط لأن `FilesScreen` تتجنبه بفرع منفصل صريح (`if (currentFolderId == null) rootFolders/allFiles else ...`). صُحح الاستعلامان ليطابقا NULL بشكل صريح (`(:x IS NULL AND x IS NULL) OR x = :x`) حتى لا يتحول هذا إلى خطأ صامت مستقبلاً لو استُخدمت الدالتان مباشرة من كود آخر (كشاشة المجرة).

## إصلاح تصميم: أيقونات اتجاهية لا تُقلب في وضع RTL

التطبيق عربي بالكامل (`android:supportsRtl="true"`)، لكن عدة أيقونات اتجاهية (تشير لليمين/اليسار دلالياً: رجوع، فتح مجلد) كانت تُستخدم من مجموعة `Icons.Filled.*` العادية التي **لا تنعكس تلقائياً** في تخطيط RTL (توثيق Compose الرسمي: هذه الأيقونات باتت مُهملة deprecated تحديداً لهذا السبب، ويجب استخدام مجموعة `Icons.AutoMirrored.Filled.*` بدلاً منها). هذا يفسّر ملاحظة "تصاميم تظهر بشكل غريب": سهم الرجوع وسهم فتح المجلد كانا يظهران بنفس اتجاههما في LTR رغم أن كامل الشاشة معكوسة RTL — مما يبدو للمستخدم كأن السهم "يشير للخارج/بالاتجاه الخطأ".

تم توحيدها في 3 شاشات:

| الملف | القديم | الجديد |
|---|---|---|
| `ui/files/FilesScreen.kt` | `Icons.Filled.ArrowBack` (شريط علوي)، `Icons.Filled.ChevronLeft` (بطاقة مجلد) | `Icons.AutoMirrored.Filled.ArrowBack`، `Icons.AutoMirrored.Filled.KeyboardArrowLeft` |
| `ui/settings/SettingsScreen.kt` | نفس الشيء (شريط علوي + سهم عناصر الإعدادات) | نفس الاستبدال |
| `ui/backup/BackupScreen.kt` | `Icons.Filled.ArrowBack` (شريط علوي) | `Icons.AutoMirrored.Filled.ArrowBack` |

(لوحظ أن `ui/dashboard/DashboardScreen.kt` كانت تستخدم بالفعل `Icons.AutoMirrored.Filled.ArrowForward` بشكل صحيح — عدم الاتساق بين الشاشات كان جزءاً من المشكلة، والآن كل الشاشات موحّدة على نفس النمط الصحيح).

تحقّق شامل نهائي (`grep`) أكّد عدم بقاء أي استخدام لأي أيقونة اتجاهية غير مُقلبة (`ArrowBack/ArrowForward/ChevronLeft/ChevronRight/KeyboardArrowLeft/KeyboardArrowRight/ExitToApp/Send/List`) من مجموعة `Icons.Filled.*` في كامل الشيفرة.

## التحقق الثابت الذي نُفّذ

- فحص توازن الأقواس `{}`/`()` آليًا لكل ملف عُدِّل (متطابقة في السبعة كلها).
- `grep` شامل يؤكد عدم وجود أي أيقونة اتجاهية متبقية بدون `AutoMirrored`.
- `grep` شامل يؤكد تحديث كل إشارة لاسم قاعدة البيانات القديم في موارد XML.
- مراجعة يدوية لكل استيراد جديد (`automirrored.filled.ArrowBack`, `automirrored.filled.KeyboardArrowLeft`, `kotlinx.coroutines.flow.catch`, `android.util.Log`) للتأكد من عدم تكراره أو تعارضه مع الاستيراد الشامل `filled.*` الموجود مسبقًا (لا تعارض: المستقبِلات receivers مختلفة: `Icons.Filled` مقابل `Icons.AutoMirrored.Filled`).
- لا كود ميت جديد: كل الدوال/الاستيرادات المضافة مُستخدمة فعليًا في نفس الملف.

## ما زال يتطلب بيئة المستخدم (Android Studio) للتأكيد النهائي

هذه البيئة لا تملك Android SDK/JDK/Gradle Wrapper، فلا يمكن تشغيل `assembleDebug` أو تثبيت APK فعلي هنا. يُنصح المستخدم بعد سحب هذه التعديلات بما يلي:

1. **إلغاء تثبيت النسخة الحالية من الجهاز/المحاكي قبل التثبيت الجديد** (أو القبول بأن قاعدة البيانات ستُبنى من جديد فارغة تحت الاسم الجديد `uni_manager_db_v2` — البيانات القديمة تحت `uni_manager_db` ستبقى غير مستخدَمة على القرص لكنها لن تُقرأ ولن تسبب أي كراش بعد الآن).
2. `./gradlew :app:assembleDebug` للتأكد من الترجمة، ثم تشغيل التطبيق والتحقق من:
   - إضافة مجلد من تبويب الملفات (جذر ثم داخل مجلد فرعي) دون كراش.
   - إضافة ملف حقيقي عبر منتقي النظام ثم فتح/إغلاق تبويب الملفات عدة مرات دون كراش.
   - ظهور سهم الرجوع وسهم فتح المجلد بالاتجاه الصحيح بصريًا (يشير نحو يمين الشاشة، أي جهة "الرجوع" في تخطيط عربي).
