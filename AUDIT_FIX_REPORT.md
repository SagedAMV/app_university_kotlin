# تقرير معالجة التدقيق التقني الشامل (UniManager-Code-Audit)

> التاريخ: 2026-09-11
> كل بند من ملف التدقيق تم التحقق منه مقابل الكود الراهن ومعالجته، مع الإشارة لما عولج في الجلسات السابقة.

## 3. أخطاء حرجة تمنع البناء

| البند | الحالة | المعالجة |
|---|---|---|
| 3.1 `Routes.X.route` على String | ✅ | أُعيد تعريف كل مسار ككائن يحمل خاصية `route` (مع `pattern/ folder()` لوسيط المجلد) |
| 3.2 تعليقات DAO يتيمة في الـ 6 ملفات | ✅ | حُذفت تعليقتا `@Query` المعلّقتان بلا دالة؛ لا توجد دالة تحمل أنوتيشن عملياتي مزدوج (فحص آلي). لم تُضَف `getById/deleteById` لأن لا شيء يستدعيها (إضافتها كود ميت يتناقض مع القسم 5.4) |
| 3.3 `booleanPreferencesName` | ✅ | استُبدلت بـ `booleanPreferencesKey` (استيراد + استخدامان) |
| 3.4 `runInTransaction` مع suspend | ✅ | استُبدلت بـ `db.withTransaction` (room-ktx) |
| 3.5 عدم تطابق اسم عدّ الامتحانات | ✅ | وُحِّد الاسم إلى `getUpcomingExamCount` في DAO و Repository معاً |

## 4. ميزات شكلية

| البند | الحالة | المعالجة |
|---|---|---|
| 4.1 ميزة الملفات وهمية | ✅ | منتقي ملفات حقيقي عبر SAF `OpenDocument`، ينسخ الملف إلى `filesDir/documents` ويسجّل الاسم/النوع/الحجم الحقيقي/MIME/المسار، وفتح فعلي عبر FileProvider مع معالجة `ActivityNotFoundException`. ونتيجة لذلك **حُذفت أذونات التخزين/الوسائط غير المستخدمة** من المانيفست و`MainActivity` (SAF والتخزين الداخلي لا يحتاجانها)، وأُبقي `POST_NOTIFICATIONS` فقط على Android 13+ |
| 4.2 `Validation` معزول | ✅ | رُبط في كل حوارات الإدخال قبل أي `insert`: المهمة (اسم/أولوية)، المحاضرة (اسم المادة/صيغة الوقت من/إلى)، الملاحظة (العنوان/المحتوى)، الامتحان (المادة/التاريخ/الوقت/النوع)، المجلد (الاسم). رسائل الخطأ تظهر داخل الحوار، والمدخلات تُنظَّف عبر `sanitizeInput` للأسماء و`sanitizeText` جديد للنصوص الطويلة (بلا قص عند 255 حرفاً مع الحفاظ على الأسطر) |
| 4.3 الترجمة معطّلة | ✅ | قرار واعٍ: التطبيق عربي بالتصميم. حُذف `values-en` (إنجليزية موهومة) و`values-ar` (تكرار)، وقُلِّص `values/strings.xml` إلى `app_name` المستخدَم فعلياً من المانيفست؛ لا ادعاءات ثنائية اللغة |

## 5. مشاكل معمارية

| البند | الحالة | المعالجة |
|---|---|---|
| 5.1 نسختا Room متضاربتان | ✅ | حُذف `AppDatabase.getInstance()` اليدوي بالكامل؛ Hilt (`DatabaseModule`) يبني القاعدة الوحيدة مع `.addMigrations(MIGRATION_1_2, MIGRATION_2_3)` فأصبحت الترقيات مفعّلة في مسار الإنتاج |
| 5.2 كسر DI | ✅ | `BackupHelper` أصبح `@Singleton @Inject constructor(context, AppDatabase)`؛ أُنشئ `BackupViewModel` (`@HiltViewModel`) تستخدمه الشاشة عبر `hiltViewModel()`؛ و`ExamNotificationScheduler` أصبح `@Singleton @Inject constructor` ويُحقن في `AppViewModel` |
| 5.3 CoroutineScope يدوي | ✅ | حُل كلياً عبر `BackupViewModel` (viewModelScope يُلغى مع دورة الحياة) |
| 5.4 مستودع Jitpack ميت | ✅ | حُذف من `settings.gradle.kts` |
| 5.4 قاعدة ProGuard لـ kotlinx.serialization | ✅ | حُذفت مع قواعد POI/Media3 لمكتبات غير مضافة |
| 5.4 `searchFiles` غير مستخدمة في UI | ⚠️ مقبولة | تبقى ضمن طبقة البيانات وهي **مُستخدمة في اختبار تكامل** (`DaoIntegrationTest.searchFiles`)؛ البحث الحالي في الذاكرة يغطي الملفات والمجلدات ضمن المجلد المفتوح بدقة سلوكية، واستبداله باستعلام SQL للملفات فقط كان سيفقد بحث المجلدات |

## 6. ملاحظات إضافية

| # | الحالة | المعالجة |
|---|---|---|
| 1 Migrations ميتة | ✅ | أصبحت مفعّلة عبر Hilt (انظر 5.1) |
| 2 أزرار الحالات الفارغة TODO | ✅ | كل تبويب يستقبل `onAdd` ويفتح حوار إضافة ذلك القسم فعلياً |
| 3 صلاحية WRITE_EXTERNAL_STORAGE / حالات الأجهزة | ✅ | حُذف مسار أذونات التخزين كاملاً |
| 4 غياب Snackbar عام في «الأقسام» | ✅ | أُضيف `SnackbarHost` يعرض رسائل `errorMessage/successMessage` في `UnifiedScreen` (كما في الرئيسية والملفات والنسخ) |
| 5 تحديث الاختبارات | ✅ | توقيعات دوال DAO لم تتغير (`insert/insertSync/...` باقية) فبقيت الاختبارات صالحة؛ أُضيف `BackupHelperTest` (instrumented) يثبت أن التصدير ثم الاستيراد ينعكس على Flow من نفس القاعدة، وحالة الملف التالف تفشل بلطف |

## قائمة DoD النهائية

- [x] المشروع يُفترض أن يُترجم: جميع أخطاء المراجع/التعليقات عولجت وراجعت الأقواس/الاستيرادات ثابتياً.
- [x] لا دالة DAO بأنوتيشن عملياتي مزدوج (فحص آلي).
- [x] تنقل متسق بنوع `route` واحد.
- [x] قاعدة بيانات واحدة (نسخة Hilt) في كل مكان بما فيه BackupHelper.
- [x] الترقيات مفعّلة في الإنتاج.
- [x] كل حوارات الإدخال تمر عبر Validation مع رسائل خطأ.
- [x] الملفات تدير ملفات حقيقية (مسار+حجم)؛ الصلاحيات الزائدة أُزيلت.
- [x] لا موارد لغات موهومة (حُذفت غير المستخدم).
- [x] الاختبارات تعكس السلوك الفعلي + اختبار نسخ احتياطي جديد.
- [ ] `assembleRelease` تحت R8: لم يُشغَّل لعدم توفر Android SDK/Gradle في بيئة المراجعة — يلزم تشغيله محلياً.

## ملاحظة بيئة العمل
لا يحتوي هذا المستودع على Gradle Wrapper ولا تتوفر أداة Gradle/Android SDK في بيئة الإعداد الحالية، فتعذّر تشغيل `./gradlew assembleDebug` والاختبارات الفعلية. أُجريت فحوص ثابتة آلية (توازن أقواس، أنوتيشنات مزدوجة، مراجع مكسورة، تكرار استيرادات/دوال). يُرجى فتح المشروع في Android Studio 2024.1+ وتشغيل:

```
./gradlew compileDebugKotlin
./gradlew testDebugUnitTest
./gradlew connectedDebugAndroidTest
./gradlew assembleRelease
```
