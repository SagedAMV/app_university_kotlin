# تقرير التدقيق العميق الثالث (UniManager — Kotlin/Android)

> التاريخ: 2026
> المنهجية: قراءة معمّقة لكل ملف Kotlin/Gradle/Manifest في المشروع (~50 ملفًا)، مقارنةً بتقارير
> التدقيق الثلاثة السابقة (`FIX_REPORT.md`, `AUDIT_FIX_REPORT.md`, `DEEP_AUDIT_FIX_REPORT.md`)
> للتأكد من عدم تكرار العمل، ثم التحقق من أن كل كود/مكتبة/دالة تخدم التطبيق فعلًا. البيئة لا
> تملك Android SDK/JDK فلا يمكن تشغيل Gradle فعليًا؛ التحقق تم عبر قراءة الكود، تتبّع الاستدعاءات،
> وتحليل ثابت (توازن الأقواس، الاستيرادات، تطابق التواقيع، ومطابقة سلوك Dagger/Hilt يدويًا).

## ملخص تنفيذي

| # | الخطورة | العنوان | الحالة |
|---|---|---|---|
| 1 | 🔴 حرج — يمنع البناء | `AppRepository` مربوطة مرتين في Dagger/Hilt (`@Inject constructor` + `@Provides` يدوي في `DatabaseModule`) | ✅ أُصلح |
| 2 | 🟠 خلل وظيفي حقيقي | استيراد نسخة احتياطية لا يُلغي/يُعيد جدولة تذكيرات WorkManager للمهام/الامتحانات | ✅ أُصلح |
| 3 | 🟡 خلل تصميمي (Compose anti-pattern) | `DatePickerField`/`TimePickerField` يُظهران حوارًا نظاميًا كأثر جانبي مباشر داخل جسم composable بدل `LaunchedEffect` | ✅ أُصلح |
| 4 | 🔵 جودة اختبارات | اختبار `validateExtension with disallowed extension returns failure` يختبر فعليًا امتدادًا **مسموحًا** ويتحقق من `isSuccess` — اسم يناقض السلوك | ✅ أُصلح |

---

## 1) 🔴 حرج: ربط مزدوج لـ `AppRepository` في Dagger/Hilt (يمنع البناء بالكامل)

**أين وُجد الخلل:**
- `app/src/main/java/com/unimanager/app/data/repository/AppRepository.kt`:
  ```kotlin
  @Singleton
  class AppRepository @Inject constructor(
      private val folderDao: FolderDao, ...
  ) { ... }
  ```
- `app/src/main/java/com/unimanager/app/di/DatabaseModule.kt` (قبل الإصلاح) كان يحوي أيضًا:
  ```kotlin
  @Provides
  @Singleton
  fun provideRepository(
      folderDao: FolderDao, fileDao: FileDao, lectureDao: LectureDao,
      taskDao: TaskDao, noteDao: NoteDao, examDao: ExamDao
  ): AppRepository {
      return AppRepository(folderDao, fileDao, lectureDao, taskDao, noteDao, examDao)
  }
  ```

**لماذا هذا خطأ حقيقي وليس أسلوبًا فقط:**
عندما تكون فئة معرَّفة بـ `@Inject constructor` (تُنشئ رابط Constructor Injection ضمنيًا)، ثم
يوجد في نفس الوقت `@Provides` صريح لنفس النوع بالضبط داخل وحدة (`Module`) مثبَّتة في نفس
المكوّن (`SingletonComponent`)، فإن Dagger/Hilt يفشل في وقت الترجمة (KSP/kapt) بالخطأ:

```
[Dagger/DuplicateBindings] com.unimanager.app.data.repository.AppRepository is bound multiple times:
    @Inject com.unimanager.app.data.repository.AppRepository(...)
    @Provides @Singleton AppRepository com.unimanager.app.di.DatabaseModule.provideRepository(...)
```

أي أن **المشروع لا يُترجَم إطلاقًا** بهذا الشكل (`./gradlew compileDebugKotlin` يفشل)، بغض النظر عن
صحة بقية الكود. هذا النوع من الأخطاء موثّق رسميًا في Dagger (راجع صفحة
`https://dagger.dev/conflicting-inject` وتقارير مطابقة على Stack Overflow) ولا يظهر إلا عند
تشغيل معالج التعليقات التوضيحية (annotation processor) الفعلي، لذا لم تلتقطه التدقيقات السابقة
الثلاث التي اعتمدت فحصًا ثابتًا فقط دون Gradle/Android SDK.

**الإصلاح المطبَّق (في `DatabaseModule.kt`):**
حُذفت دالة `provideRepository` بالكامل، وأُبقي فقط على مزوّدات الـ DAOs الستة
(`provideFolderDao`, `provideFileDao`, ...) التي تحتاجها `AppRepository` عبر `@Inject
constructor` الخاص بها. النتيجة: رابط وحيد لـ `AppRepository` في الرسم البياني بالكامل.

كذلك حُذف الاستيراد غير المستخدم `import com.unimanager.app.data.repository.AppRepository`
من `DatabaseModule.kt` بعد إزالة الدالة (لم يعد له أي استخدام في الملف).

---

## 2) 🟠 خلل وظيفي: تذكيرات المهام/الامتحانات تُهمَل بعد استيراد نسخة احتياطية

**أين وُجد الخلل:** `app/src/main/java/com/unimanager/app/backup/BackupHelper.kt` — دالة
`importBackup`.

**وصف المشكلة:**
الاستيراد كان يحذف كل صفوف المهام/الامتحانات القديمة ثم يستورد الصفوف الجديدة **داخل قاعدة
البيانات فقط**، دون أي تفاعل مع `ExamNotificationScheduler` (طبقة WorkManager المسؤولة فعليًا
عن التذكيرات المعروضة في القنوات `exams`/`tasks`). العواقب الحقيقية على المستخدم:

1. أي تذكير كان مجدولاً قبل الاستيراد لعنصر تم حذفه/استبداله يبقى حيًّا في WorkManager وقد
   يُطلق إشعارًا لاحقًا لا معنى له (بيانات لم تعد موجودة).
2. المهام/الامتحانات المستوردة حديثًا وتملك موعد استحقاق مستقبلي **لا تحصل على أي تذكير
   إطلاقًا** إلا إذا فتحها المستخدم يدويًا وعدَّلها لاحقًا. أي أن ميزة "النسخ الاحتياطي" كانت
   تُرجع البيانات بصريًا في الواجهة لكنها **تُسقط بصمت** الوعد الوظيفي بالتذكير.

**الإصلاح المطبَّق:**
- حُقن `ExamNotificationScheduler` في `BackupHelper` عبر الـ constructor (Hilt يوفّره تلقائيًا
  لأنه `@Singleton @Inject constructor` أصلاً — لا حاجة لأي تعديل في DI module).
- قبل حذف الجدول، تُلتقط معرّفات المهام/الامتحانات الحالية.
- بعد نجاح الـ transaction (حذف + استيراد)، تُلغى كل التذكيرات القديمة بهذه المعرّفات، ثم
  تُعاد جدولة تذكيرات فعلية لكل مهمة مستوردة تملك `dueDate`، ولكل امتحان مستورد، مع تغليف
  الخطوة بـ `runCatching` حتى لا يُفشل نجاح الاستيراد نفسه إن تعذّرت الجدولة لسبب خارجي.

**أثر جانبي على الاختبارات:** غيّر هذا توقيع مُنشئ `BackupHelper` (أصبح يتطلب معاملاً ثالثًا)،
فحُدِّث `BackupHelperTest.kt` ليبني `ExamNotificationScheduler(context)` مباشرة عند الإعداد.

---

## 3) 🟡 خلل تصميمي في Compose: إظهار حوار نظامي كأثر جانبي مباشر أثناء التركيب

**أين وُجد الخلل:** `app/src/main/java/com/unimanager/app/ui/components/DateTimePickers.kt` —
`DatePickerField` و`TimePickerField`.

**وصف المشكلة:** كان الكود يبني `DatePickerDialog`/`TimePickerDialog` ويستدعي `.show()` ثم
مباشرة `showDialog = false` **داخل جسم دالة composable نفسها** (وليس داخل `LaunchedEffect`).
هذا مخالف لعقد Jetpack Compose، والخطر العملي: أي إعادة تركيب إضافية بينما `showDialog` لا تزال
`true` قد تُنشئ وتعرض نسخة **ثانية** من الحوار فوق الأولى، وكانت إعادة الضبط تحدث فورًا وليس
عند الإغلاق الفعلي من المستخدم.

**الإصلاح المطبَّق:** نُقل إنشاء/عرض الحوار داخل `LaunchedEffect(showDialog)`، وأُضيف
`setOnDismissListener { showDialog = false }` بحيث تُعاد الحالة فعليًا عند إغلاق الحوار
(اختيار قيمة أو إلغاء)، بدل إعادة الضبط الفورية غير المشروطة. طُبِّق نفس المنطق على الحقلين.

---

## 4) 🔵 جودة اختبارات: اسم اختبار يناقض سلوكه الفعلي

**أين وُجد الخلل:** `app/src/test/java/com/unimanager/app/util/ValidationTest.kt`.

كان يوجد اختبار باسم `validateExtension with disallowed extension returns failure` لكن جسمه
يستخدم الامتداد `"exe"` الموجود فعلاً ضمن `Validation.ALLOWED_EXTENSIONS`، والتأكيد داخله هو
`assertTrue(result.isSuccess)`. نتيجة ذلك: لا يوجد أي اختبار فعلي يغطي مسار رفض امتداد غير
مسموح. **الإصلاح:** تقسيم الاختبار إلى اثنين بأسماء مطابقة لسلوكها الفعلي (أحدهما "exe" ناجح،
والآخر "xyz" فاشل فعليًا).

---

## ما تم فحصه ولم يظهر فيه أي خلل يستدعي التعديل

- **طبقة البيانات** (entities/DAOs/Repository/AppDatabase والهجرات 1→2→3→4): متّسقة، الفهارس
  ومفتاح FK (`files.folderId → folders.id` مع `CASCADE`) صحيحان، ولا استعلامات ميتة.
- **`AppViewModel`**: كل العمليات تمر بمعالجة أخطاء، وحذف الملفات/المجلدات يحذف الملفات الفعلية
  من التخزين الداخلي فعلاً، وجدولة/إلغاء التذكيرات عند الإضافة/التعديل/الحذف اليدوي صحيحة.
- **`ExamNotificationScheduler`/`NotificationWorker`**: معرّفات WorkManager فريدة، القنوات
  (`exams`/`tasks`) تُنشأ فعليًا وتُستخدم بالاسم نفسه في الجدولة والعرض.
- **`FilesScreen`**: اختيار الملفات عبر SAF حقيقي (نسخ فعلي، تحقق من الامتداد، منع تصادم
  الأسماء، فتح عبر FileProvider مع معالجة `ActivityNotFoundException`). لا "ميزات وهمية".
- **`UnifiedScreen`**: كل تبويب يدعم إضافة + تعديل + حذف (مع تأكيد موحّد) والتحقق يمر عبر
  `Validation` قبل أي حفظ.
- **`GalaxyScreen`**: التنقل من عقدة المجرة إلى شاشة الملفات لمجلد محدد يعمل فعليًا.
- **`build.gradle.kts`**: كل الاعتماديات مستخدمة فعليًا؛ لا مكتبات ميتة.
- **`AndroidManifest.xml`/`proguard-rules.pro`**: إذن واحد فقط (`POST_NOTIFICATIONS`) متوافق مع
  تصميم SAF بلا صلاحيات تخزين.
- لا `TODO`/`FIXME`/`GlobalScope`/استخدامات `!!` خطرة في كامل شجرة `app/src` (تحقق عبر grep شامل).

---

## قائمة الملفات المعدَّلة في هذه الجلسة

| الملف | نوع التعديل |
|---|---|
| `app/src/main/java/com/unimanager/app/di/DatabaseModule.kt` | حذف `@Provides` مكرِّر لـ `AppRepository` (إصلاح حرج يمنع فشل البناء) + حذف استيراد غير مستخدم |
| `app/src/main/java/com/unimanager/app/backup/BackupHelper.kt` | حقن `ExamNotificationScheduler` + إلغاء/إعادة جدولة تذكيرات المهام والامتحانات بعد الاستيراد |
| `app/src/androidTest/java/com/unimanager/app/backup/BackupHelperTest.kt` | تحديث بناء `BackupHelper` بالمعامل الجديد `ExamNotificationScheduler` |
| `app/src/main/java/com/unimanager/app/ui/components/DateTimePickers.kt` | نقل إظهار `DatePickerDialog`/`TimePickerDialog` إلى `LaunchedEffect` بدل الأثر الجانبي المباشر أثناء التركيب |
| `app/src/test/java/com/unimanager/app/util/ValidationTest.kt` | تصحيح اختبار `validateExtension` المضلِّل إلى اختبارين متوافقين مع أسمائهما |

## خطوات التحقق المتبقية على جهاز المطوّر (لا تتوفر بيئة Android SDK هنا)

```bash
./gradlew compileDebugKotlin   # يجب أن ينجح الآن بعد حل تعارض Dagger/Hilt (البند 1)
./gradlew testDebugUnitTest    # يشمل اختباري validateExtension الجديدين
./gradlew connectedDebugAndroidTest   # يشمل BackupHelperTest المحدَّث و DaoIntegrationTest
./gradlew assembleRelease      # للتأكد من نجاح R8/ProGuard كذلك
```
